//Crie um formulário com um campo de texto e um botão de envio. Quando o botão for clicado, exiba um alerta com o que foi digitado.

function enviar(){
    var a = document.getElementById("texto").value;
  alert(a)
}