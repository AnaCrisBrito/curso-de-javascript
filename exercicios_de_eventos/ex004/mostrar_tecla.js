//Use o evento keydown para mostrar no console qual tecla o usuário pressionou no teclado.
  document.addEventListener("keydown", function(event) {
    console.log("Tecla pressionada:", event.key);
  });